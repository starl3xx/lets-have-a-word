{
  "name": "lhaw-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Let's Have A Word game on Base",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:base-sepolia": "hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy.ts --network base",
    "deploy:word-manager:base-sepolia": "hardhat run scripts/deploy-word-manager.ts --network baseSepolia",
    "deploy:word-manager:base": "hardhat run scripts/deploy-word-manager.ts --network base",
    "verify:base-sepolia": "hardhat verify --network baseSepolia",
    "verify:base": "hardhat verify --network base",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@openzeppelin/contracts-upgradeable": "^5.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}
