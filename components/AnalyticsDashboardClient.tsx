// components/AnalyticsDashboardClient.tsx
"use client"

import React from "react"

const AnalyticsDashboardClient: React.FC = () => {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "system-ui, -apple-system, BlinkMacSystemFont, sans-serif",
      }}
    >
      <div>
        <h1>Let's Have A Word – Analytics</h1>
        <p>Baseline placeholder: no charts, no SIWN, no data yet.</p>
      </div>
    </main>
  )
}

export default AnalyticsDashboardClient
